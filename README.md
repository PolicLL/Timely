When creating the project, I was using Postgresql db. Commands for creating table I used are in schema.sql file. Path to the database can be found in application.properties at
property named spring.datasource.url.
