When creating the project, I used Postgresql. Commands for creating table I used are in schema.sql file. Path to the database can be found in application.properties at
property named spring.datasource.url.
